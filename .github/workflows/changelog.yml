name: Changelog

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  changelog:
    if: github.event.pull_request.head.repo.full_name == github.repository
    uses: ignazio-ingenito/developer-workspace/.github/workflows/changelog-reusable.yml@bbdc8e05ffbaaf8d296c02c883b780391d3655c9
    with:
      app-client-id: ${{ vars.CHANGELOG_APP_CLIENT_ID }}
    secrets:
      app-private-key: ${{ secrets.CHANGELOG_APP_PRIVATE_KEY }}
